name: Auto Build & Push To Amazon ECR

on:
  push:


jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    steps:
      - name: test
        run: |
            exit 0
